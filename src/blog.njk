---
layout: layouts/base.njk
title: Blog Archive
description: Blog archive. 
---

<main class="container flow">
    <h1>{{title}}</h1>
    
    <ol class="blog-list flow">
        {% for post in collections.posts %}
            <li class="flow">
                <h2><a href="{{post.url}}">{{post.data.title}}</a> | <small><small>
                    <time datetime="{{ post.date | readableDate("yyyy-LL-dd") }}">{{ post.date | readableDate("LLLL dd, yyyy") }}</time>
                </small></small></h2>
                <p>{{post.data.description}}</p>
            </li>
        {% endfor %}
    </ol>
</main>